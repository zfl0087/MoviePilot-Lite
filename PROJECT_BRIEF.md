# MoviePilot Lite — Project Brief

> 状态：已建立固定能力配置，API 路由导入前门控实施中
> 基线日期：2026-07-17  
> 项目性质：基于 MoviePilot v2 的非官方私有精简构建

## 1. 项目概述

MoviePilot Lite 是面向网盘媒体管理场景的 MoviePilot 精简版本。项目保留网盘、元数据识别、文件整理、插件、消息通知和媒体服务器能力，移除 PT、下载器、站点订阅及其他不属于目标场景的运行链路。

精简目标不是只隐藏前端入口，而是在构建和运行时真正做到：不安装非目标依赖、不导入和注册非目标模块、不启动相关服务与定时任务，同时尽量保留上游源码结构，降低持续合并官方更新的成本。

## 2. 目标用户与分发范围

- 主要用于个人和家庭自托管。
- 允许向少量明确、可信的对象私下分享。
- 不公开发行，不提供公众下载，不作为 SaaS 或商业产品运营。
- 向他人分发镜像时，同时提供与镜像版本对应的完整源码，保留 GPLv3 许可证、版权声明和修改记录。
- 每个版本必须能够追溯到对应的 MoviePilot 官方提交和 MoviePilot Lite 源码提交。

## 3. 产品目标

1. 形成以网盘媒体处理为核心的完整可用产品，而不是官方版的菜单隐藏方案。
2. 显著降低空闲内存、镜像体积、启动时间和无效后台活动。
3. 持续跟踪 MoviePilot 官方稳定版本，并控制精简补丁规模。
4. 保持官方 `/config` 数据目录的迁移与回退能力。
5. 保持必要的安全边界、消息权限和插件扩展能力。
6. 以 115 网盘作为首个版本的真实验收对象，同时保留其他官方网盘适配器的源码兼容性。

## 4. 核心使用流程

### 4.1 网盘媒体处理

```text
网盘文件或目录
  → 元数据识别与人工纠正
  → 重命名、刮削和文件整理
  → STRM 或媒体库内容更新
  → Emby / Jellyfin / Plex 刷新
```

### 4.2 消息发送 115 分享链接

```text
官方消息渠道接收分享链接
  → 沿用官方渠道身份与权限判断
  → MoviePilot Lite 核心识别 115 链接
  → 调用用户手动安装的 P115StrmHelper
  → 完成转存及后续 STRM 处理
  → 通过原消息渠道返回结果
```

P115StrmHelper（115网盘STRM助手）是目标 115 工作流的必装插件，但必须由用户手动安装；MoviePilot Lite 不预装、不自动安装，也不在后台自动下载插件。插件未安装或未运行时，系统正常启动，但相关入口应明确提示缺少该插件。

## 5. 保留范围

### 5.1 网盘与文件能力

- 保留官方已有的网盘及文件存储适配器源码和兼容性。
- 仅加载实际配置、启用的适配器。
- 重点支持并真实验证 115 网盘授权、分享链接转存、文件整理、STRM、媒体库刷新和异常重试。
- 保留文件浏览、文件管理、目录监控、手动整理、整理历史和失败重试。

### 5.2 元数据与整理

- 保留 TMDB、豆瓣、Bangumi、TVDB、Fanart 等识别和刮削底层能力。
- 保留识别预览、人工纠正、重命名、刮削和整理能力。
- 元数据能力只服务于识别、整理和媒体库管理。

### 5.3 插件

- 保留官方插件市场、插件安装与升级、插件页面、事件、定时任务和插件 API。
- 插件由用户手动安装。
- P115StrmHelper 列入明确支持名单。
- 依赖 PT、站点、下载器、订阅、Agent、Redis 或工作流等已移除能力的插件，应在安装或启用前显示不兼容原因并阻止运行。
- 其他插件只有通过兼容性测试后才能进入支持名单。

### 5.4 消息与通知

- 保留官方消息渠道、通知发送、必要回调和插件消息动作。
- 保留接收 115 分享链接并自动转存的入站链路。
- 删除远程 PT 搜索、订阅、下载和 AI 对话等命令分支。
- 消息渠道的用户白名单、管理员白名单、用户 ID 绑定、命令权限和默认允许/拒绝行为全部保持官方逻辑，不新增或重写权限模型。

### 5.5 媒体服务器

- Emby、Jellyfin、Plex 集成能力与官方版本保持一致。
- 保留媒体库刷新、同步、查询和官方现有的相关交互能力。

### 5.6 安全与接口

- 保留单管理员账号、密码登录、登录 Token 和会话安全。
- 保留 `API_TOKEN` 和必要的 REST API，供插件、通知回调及外部集成使用。
- 保留 115 所需的 `U115_APP_ID`、`U115_AUTH_SERVER` 及相关 OAuth/API 链路；它们不属于 PT 用户认证。
- 真实网盘令牌只保存在部署实例的安全配置中，不进入源码或普通 CI 环境。

### 5.7 运行基础

- 正式支持 SQLite。
- 缓存固定为进程内本地缓存。
- 保留 `ffprobe`，用于视频信息读取和识别整理。
- 支持 Docker `linux/amd64` 和 `linux/arm64`。

## 6. 移除范围

- PT 站点、索引器、站点资源包及其搜索链路。
- `AUTH_SITE`、PT 认证入口及在线校验链路。
- qBittorrent、Transmission、rTorrent 等下载器及其任务、种子和下载处理能力。
- 影视订阅、资源搜索、下载中、热门推荐、探索、榜单、人物搜索和内容发现页面。
- AI Agent、LLM、MCP、Skills、语音处理及相关模型依赖。
- 工作流编辑器、执行引擎及其后台任务；插件自身的事件和定时任务不在此列。
- CookieCloud、CloakBrowser、Playwright 浏览器内核、虚拟显示、验证码 OCR 和 FlareSolverr。
- PostgreSQL、Redis、多用户、用户注册、角色系统、SSO 和辅助认证（`auxiliary-auth`）。
- Radarr/Sonarr 兼容接口及 CookieCloud 接口。
- `ffmpeg`；依赖 FFmpeg 转码的第三方插件标记为不兼容。
- 非必要的官方统计和数据上报；只保留元数据、插件市场、网盘授权/API 等明确必要的外联。

## 7. 架构原则

- 保留官方源码结构，避免大规模物理删除造成长期合并失控。
- 建立统一的 Lite capability profile，并在模块导入之前控制后端模块、API、Schema、定时任务、命令、依赖及前端路由。
- 前端和后端使用同一份能力定义，避免只隐藏菜单或两边分别维护删除清单。
- 未启用能力不得注册路由、实例化服务、创建后台任务或安装专属依赖。
- 被裁剪模块的历史配置和未知数据默认保留，不主动删除。
- 首次使用官方 `/config` 启动 Lite 前必须备份数据库，并保留退回官方版本的可能。

## 8. 上游同步与发布

- 官方后端上游：`jxxghp/MoviePilot`，跟踪 `v2`。
- 官方前端上游：`jxxghp/MoviePilot-Frontend`，跟踪 `v2`。
- 官方插件上游：`jxxghp/MoviePilot-Plugins`。
- 私有后端仓库：`zfl0087/MoviePilot-Lite`。
- 私有前端仓库：`zfl0087/MoviePilot-Lite-Frontend`。
- 不使用 GitHub 的公开 Fork 关系；使用私有镜像仓库并配置官方仓库为上游。
- 上游稳定版本到达后，自动同步、测试并构建候选镜像。
- 测试失败时停止发布，不让运行实例自动盲目升级。
- 候选镜像先在 canary 实例真实验证，再由用户手动升级其他实例。
- Compose 等部署配置固定具体版本；不得仅依赖浮动的 `latest`。
- 版本格式采用 `v<官方版本>-lite.<修订号>`，例如 `v2.14.5-lite.1`。

## 9. 验收指标

所有资源指标必须在同一设备、同一基础配置下，与相同官方版本对比：

- 空闲常驻内存至少降低 30%。
- 空闲 CPU 长期接近 0。
- Docker 镜像体积至少降低 25%。
- 冷启动时间至少缩短 20%。
- 未启用网盘监控时，不产生周期性网盘扫描。
- 不运行站点、订阅、下载器、Agent 或工作流轮询。

功能验收至少覆盖：启动、管理员登录、115 授权、分享链接转存、识别整理、P115StrmHelper 加载、通知、媒体服务器刷新、升级前备份和失败回退。

CI 使用模拟服务验证通用流程；真实 115 Token 不进入 GitHub Actions。最终候选版本必须在 canary 实例完成一次真实转存和整理验证。

## 10. 非目标

- 不重新设计 MoviePilot 的整体视觉语言和基础交互。
- 不承诺兼容所有官方或第三方插件。
- 不为已移除能力保留完整核心，仅为了兼容个别插件而恢复相关依赖。
- 不维护 Windows 原生、群晖 SPK 或本地 CLI 安装包。
- 不公开发布或建立公共下载渠道。
- 不在首版中为所有网盘进行真实账号验收。

## 11. 当前基线与状态

已完成上游基线审计，首个 Lite 实施基线如下：

| 组件 | 分支 | 基线提交 |
|---|---|---|
| MoviePilot 后端 | `v2` | `8b5524a321c940f337873cefa54ba6a58462f9a4` |
| MoviePilot 前端 | `v2` | `435e9ecfdd4febf791fd581b3be9233816cebf7f` |
| MoviePilot 官方插件 | `main` | `4e11007980fd7703302adfa4474e494b64244b5c` |

两个私有仓库已经创建，并且 Codex GitHub App 仅获准访问这两个仓库。后端 `lite` 分支已建立固定能力配置、API 路由和模块发现的导入前门控；当前已在运行时移除 `AUTH_SITE`、PT 在线认证、Agent、Workflow、Redis、订阅、搜索和下载器的启动链路。管理员登录、`API_TOKEN` 和资源 Token 固定使用本地等级 1，115 OAuth/API 保持独立。启动期只装配模块、事件、已安装插件、Lite Scheduler、Monitor、Lite Command 和条件 DoH；不会自动同步插件、安装插件依赖或上报使用统计。内建消息命令固定为 `/mediaserver_sync`、`/clear_cache`、`/restart`、`/version`，普通 115 分享链接继续广播给手动安装的 P115StrmHelper。Scheduler 只保留媒体服务器同步、公共维护、缓存清理、条件数据清理、条件主动 GC 和兼容插件任务。依赖、Docker 和前端裁剪仍待后续独立变更，尚未构建或发布 Lite 镜像。

## 12. 项目协作规则

- 普通实施步骤可以连续推进；在运行代码实施、Git 提交、推送、发布和生产升级等关键节点前必须讨论并获得用户确认。
- 每个关键节点完成后停止并汇报证据，不把当前批准自动扩大到下一关键节点。
- 不把 GitHub 密码、115 Cookie/Token、机器人 Token、MoviePilot 密码、`API_TOKEN` 或真实 `/config` 提交到仓库。
- 需求和验收规则后续使用 OpenSpec 按变更增量管理；本文件只维护稳定的项目方向与边界。
- Product Design 当前不启用；只有发生显著的信息架构或交互重设计时才讨论使用。

## 13. 后续需要形成的文档

- `AGENTS.md`：AI 开发约束、禁止事项和验证规则。
- `docs/ARCHITECTURE.md`：Lite capability profile、依赖切割和前后端边界。
- `docs/UPSTREAM_SYNC.md`：分支、上游同步、冲突处理和发布流程。
- `docs/COMPATIBILITY.md`：插件、配置、API 和迁移兼容矩阵。
- `openspec/`：每项具体改动的提案、任务、规范增量和验收场景。
