## 1. 只读审计、边界与批准

- [x] 1.1 严格验证 `gate-lite-frontend-build` 的 proposal、design、specs 和 tasks，并与后端 capability profile version 2、兼容矩阵和上游同步规则对照
- [x] 1.2 在前端仓库审计路由、菜单、应用中心、搜索入口、首页重定向、PWA、Service Worker、模块联邦和动态导入，输出完整保留/移除矩阵
- [x] 1.3 对 `package.json` 和 lockfile 建立依赖使用点矩阵，区分禁用专属、保留共享、构建工具和插件联邦依赖
- [x] 1.4 确认前端 `AGENTS.md` 的未跟踪状态、权威后端文档配对和两个仓库的基线提交；不把该文件或其他临时文件无意带入实现提交
- [x] 1.5 在修改前端或后端代码前展示 manifest 传递、路由注册、构建裁剪和测试方案，取得用户明确实施批准

## 2. 能力清单与配对合同

- [x] 2.1 以 `get_lite_capability_manifest()` 为唯一来源定义前端构建 manifest 输入和 schema，禁止前端维护第二份启用列表
- [x] 2.2 将非敏感 profile/version 配对信息接入现有 `/api/v1/system/global` 响应，并覆盖缺失、未知和版本不匹配的失败关闭测试
- [x] 2.3 实现前端构建在 manifest 缺失、格式错误或能力集合不完整时失败关闭；测试 fixture 与生产输入明确分离

## 3. 路由、菜单、PWA 与插件实现

- [x] 3.1 将路由注册改为能力筛选后的集中 registry，保留官方相对路径、权限、页面守卫和默认首页语义
- [x] 3.2 让 `i18n-menu.ts`、应用中心、搜索面板、快捷入口和首页重定向消费同一筛选结果，移除禁用入口而不新增第二清单
- [x] 3.3 按能力裁剪 PWA manifest、快捷方式、Service Worker 预缓存和离线导航回退，保留更新提示和缓存清理语义
- [x] 3.4 保留模块联邦和插件动态路由；覆盖 P115StrmHelper 缺失、依赖失败、兼容通过和远程组件加载失败状态
- [x] 3.5 为历史禁用 URL、未知 capability、前后端版本不匹配和权限拒绝实现多语言、可诊断的失败状态

## 4. 构建依赖与产物裁剪

- [x] 4.1 逐项删除仅服务 PT、下载、订阅、发现、Agent、Workflow、辅助认证或多用户页面的生产依赖；共享/构建/插件依赖必须保留并记录
- [x] 4.2 更新生产构建入口和产物检查，证明禁用页面、动态 chunk、专属图标和 Service Worker 预缓存不进入 bundle
- [x] 4.3 保留登录、网盘、识别整理、历史、插件、通知、媒体服务器和必要设置的页面、资源和依赖
- [x] 4.4 不把 P115StrmHelper 或其他插件源码复制到核心构建，不改变官方视觉语言和无关页面布局

## 5. 前端验证与兼容

- [x] 5.1 新增路由/菜单能力矩阵、manifest schema、配对版本、PWA 资源和历史 URL 测试，全部使用 mock，不连接真实后端或外部服务
- [x] 5.2 运行 `yarn typecheck`、`yarn test:run` 和 `yarn build`，记录 Node/Yarn、前端提交、后端配对提交和构建摘要
- [x] 5.3 对构建产物执行禁用路径、依赖、chunk、PWA 预缓存和插件源码扫描；失败关闭，不生成候选产物
- [x] 5.4 在配对后端上验证管理员登录、网盘、识别整理、插件、消息通知和媒体服务器基本导航；真实 115 留待 Canary
- [x] 5.5 更新前端/后端兼容文档和版本配对记录，不把未通过 Docker 或 Canary 的前端标记为正式支持

## 6. 版本控制检查点

- [x] 6.1 在创建前端或后端实现提交前停止，展示差异、依赖矩阵、测试、产物合同和已知风险并取得用户明确提交批准
- [ ] 6.2 获批后创建单一目的前端能力门控提交；如需后端配对字段，使用独立且可追溯的后端提交
- [ ] 6.3 提交后停止，分别展示前端和后端提交、远程状态及产物摘要，取得用户推送批准
- [ ] 6.4 仅推送到私有 `origin`；不得向两个 `upstream` 推送，不创建公开 PR，不构建或发布 Docker 镜像
