## 1. 规划与契约

- [x] 1.1 严格验证 `gate-lite-api-routes` 的 proposal、design、specs 和 tasks 工件
- [x] 1.2 确认实现边界仅覆盖 API 导入与注册，不包含前端、插件、依赖、后台任务、Docker 或数据库改动

## 2. 契约测试

- [x] 2.1 更新能力配置测试，先覆盖 `auxiliary-auth` 禁用分类和配置版本 2
- [x] 2.2 新增主 API 保留/禁用路径矩阵及相对顺序测试
- [x] 2.3 新增禁用端点、Radarr/Sonarr 和 CookieCloud 模块在正常初始化中未导入的隔离测试
- [x] 2.4 新增缺失禁用模块仍能初始化、缺失保留模块明确失败的测试，并确认进程级 import 状态完整恢复

## 3. 后端实现

- [x] 3.1 在固定能力配置中新增 `auxiliary-auth` 并将配置版本递增到 2
- [x] 3.2 将主 API 改为能力检查后的有序延迟导入，保持启用路由的路径、标签和相对顺序
- [x] 3.3 在启动路由初始化器中于 import 前门控 Radarr/Sonarr 与 CookieCloud 独立接口

## 4. 范围与兼容审查

- [x] 4.1 确认管理员登录、`API_TOKEN`、115 OAuth/API、插件、消息通知和媒体服务器路由仍保留既有契约
- [x] 4.2 确认前端、插件兼容清单、依赖文件、Docker、数据库和 `/config` 未发生本变更范围外修改
- [x] 4.3 记录保留端点内部历史耦合及前端/服务/任务/依赖裁剪为后续独立变更，不在本次实现中扩大范围

## 5. 验证

- [x] 5.1 运行能力配置和 API 路由定向 pytest，保存通过结果
- [x] 5.2 对修改的 Python 模块运行定向 pylint，确认没有新增问题
- [x] 5.3 因涉及 `app/core/` 和启动流程，运行全量 pytest 并与已知 Windows 基线失败比较
- [x] 5.4 运行 `pylint app/`，区分本变更新问题和已知 Windows `fcntl` 基线问题
- [x] 5.5 严格验证已完成的 OpenSpec 变更并记录验证证据；本变更不使用真实 115 Token 或外部服务

## 6. 版本控制检查点

- [x] 6.1 在创建 Git 提交前停止，展示修改、测试结果和风险，取得用户明确批准
- [x] 6.2 获批后创建单一目的提交
- [x] 6.3 提交后停止，展示提交信息并取得用户明确推送批准
- [x] 6.4 获批后仅推送到私有 `origin/lite`；不得推送 `upstream`、发布版本或升级生产实例
