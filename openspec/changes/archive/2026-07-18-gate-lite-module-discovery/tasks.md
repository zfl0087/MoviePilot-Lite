## 1. 规划、基线与批准

- [x] 1.1 严格验证 `gate-lite-module-discovery` 的 proposal、design、specs 和 tasks 工件，并确认包矩阵与正式产品文档一致
- [x] 1.2 在干净测试进程记录当前模块发现的导入包、耗时、RSS 和活动线程基线，证明顶层全扫描及全部存储适配器导入问题
- [x] 1.3 核对当前分支、工作树、全量 pytest 已知 Windows 失败和 Pylint 基线，不把既有问题误归因于本变更
- [x] 1.4 在修改运行代码前展示固定模块矩阵、存储延迟加载、统一热重载、首次 115 授权和 API 集合变化，并取得用户明确批准

## 2. 导入前门控契约测试

- [x] 2.1 新增 pytest-native 的 `ModuleHelper` 包过滤测试，证明拒绝与过滤异常均发生在 import/reload 前且默认调用方兼容
- [x] 2.2 新增顶层模块矩阵参数化测试，覆盖无条件保留、条件允许、固定拒绝、历史配置无效和未知包 fail-closed
- [x] 2.3 新增缺失禁用依赖的隔离测试，阻断 qBittorrent、Transmission、Redis、PostgreSQL、PT 及非目标媒体服务器导入仍可完成发现
- [x] 2.4 新增消息和媒体服务器条件配置测试，证明未启用不导入、启用类型只导入一次、缺失已启用依赖只隔离该包

## 3. 通用加载器与顶层模块实现

- [x] 3.1 为 `ModuleHelper.load()` 增加包级预过滤类型与实现，补全中文公开 docstring，并以 2.1 测试验证调用顺序
- [x] 3.2 在 ModuleManager 定义固定顶层包矩阵和只读配置选择逻辑，禁止将配置 type 拼接为导入路径
- [x] 3.3 将顶层包过滤接入 `app.modules` 扫描，保持允许模块的类过滤、优先级、错误隔离和初始化契约
- [x] 3.4 验证空条件配置下只发现六个核心包，旧 Downloaders、Redis 和非目标媒体服务器配置不能恢复禁用模块

## 4. 集合配置生命周期

- [x] 4.1 新增 Notifications、MediaServers、Storages 配置变化的 stop/init 计数、停止异常、并发串行和 ModuleReload 事件测试
- [x] 4.2 让 ModuleManager 使用可重入锁统一监听并重建三类配置集合，失败模块不进入新运行集合
- [x] 4.3 移除消息与媒体服务器基类对对应集合配置的重复重载所有权，保留模块专属环境配置监听
- [x] 4.4 验证新增允许类型只初始化一次、停用类型停止且不再处理业务事件、运行中已导入代码不被不安全强制卸载

## 5. 存储适配器第二层门控

- [x] 5.1 新增 Local 兜底、仅 U115 配置、多个官方存储、未知 type 和普通 FileItem 不得触发未配置导入的 pytest 用例
- [x] 5.2 为 FileManager 建立固定存储 type 到包名映射，并在存储包导入前只允许 Local 与已配置远端类型
- [x] 5.3 新增首次配置、二维码、OAuth 和登录确认的精确按需加载测试，覆盖有效凭据、权限拒绝和未知类型失败关闭
- [x] 5.4 实现固定官方存储的单目标按需加载，保证 `/storage/auth_url/u115` 与既有 115 OAuth/API 语义不变且不扫描其他存储
- [x] 5.5 确保存储专用保存和重置发布 Storages 配置变化，并验证活动 FileManager 最终与 SQLite 配置一致
- [x] 5.6 使用完全模拟的 115 响应验证授权、配置重载、文件操作和重启恢复；真实 115 Token 只留给后续候选 Canary 人工验证

## 6. API、前端与插件兼容

- [x] 6.1 扩展 `/system/modulelist` 和 `/system/moduletest/{moduleid}` 测试，验证实际 Lite 集合、既有字段/i18n、失败语义和 Token 权限不变
- [x] 6.2 回归配对前端“先保存 Storages/Notifications/MediaServers，再授权或测试”的调用顺序，确认前端配置流程不依赖未加载模块出现在 modulelist
- [x] 6.3 验证插件管理器、插件事件和手动安装策略未被模块门控改变，P115StrmHelper 缺失时核心启动仍不导入或自动下载插件
- [x] 6.4 更新 `PROJECT_BRIEF.md`、`docs/ARCHITECTURE.md`、`docs/COMPATIBILITY.md` 和 `docs/mcp-api.md`，记录模块矩阵、API 响应集合、热停用边界及实际完成状态
- [x] 6.5 确认本变更不修改前端仓库、数据库 Schema、依赖锁文件或 Docker 构建；输出可供后续依赖和镜像裁剪使用的安全删除候选证据

## 7. 质量、集成与资源验证

- [x] 7.1 运行模块 Helper、ModuleManager、FileManager、系统模块 API、配置事件和生命周期定向 pytest，确保测试全程零真实出站
- [x] 7.2 对所有修改的 Python 模块运行定向 Pylint，确认没有新增错误且公开接口中文 docstring 完整
- [x] 7.3 因修改 `app/core/`、共享 Helper、模块基类和启动发现路径，运行全量 pytest 并逐项对比已知 Windows 基线
- [x] 7.4 运行 `pylint app/`，区分本变更新问题与已知 Windows `fcntl` 基线问题
- [x] 7.5 在相同解释器、依赖和配置下复测导入包数、模块发现耗时、RSS 和活动线程，与 1.2 基线对比并记录结论
- [x] 7.6 严格验证完成后的 OpenSpec，运行 `git diff --check` 和秘密信息检查；本变更不改依赖，因此不运行依赖安全扫描
- [x] 7.7 使用官方 `/config` 副本完成离线启动、配置启停、停止与再次启动集成测试，不改写真实生产配置或连接外部服务

## 8. 版本控制检查点

- [x] 8.1 在创建实现提交前停止，展示改动范围、测试结果、资源差异、已知基线和仍待裁剪的启动/任务链路，取得用户明确批准
- [x] 8.2 获批后创建单一目的实现提交，并在推送前再次停止取得明确批准
- [x] 8.3 获批后仅推送到私有 `origin/lite`；不得推送 `upstream`、发布版本、构建公开镜像或升级生产实例
- [x] 8.4 实现提交推送后归档 OpenSpec；归档提交和推送分别再次遵守用户批准检查点
