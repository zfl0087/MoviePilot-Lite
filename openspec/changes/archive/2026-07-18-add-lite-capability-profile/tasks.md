## 1. 实施批准与基线

- [x] 1.1 在开始修改代码前获得用户对 proposal、spec 和 design 的明确实施批准
- [x] 1.2 记录后端分支、基线提交和工作区状态，确认现有用户改动不被覆盖

## 2. 后端能力契约测试

- [x] 2.1 新增能力分类测试，精确验证启用集合、禁用集合、互斥性和完备性
- [x] 2.2 新增不可变性和严格查询测试，验证环境配置不能恢复禁用能力且未知输入安全失败
- [x] 2.3 新增机器清单测试，验证固定字段、稳定排序、重复生成一致和返回值隔离
- [x] 2.4 新增导入隔离测试，验证能力模块只依赖标准库且不会加载业务模块或触发外部副作用
- [x] 2.5 运行新增测试并保留实施前预期失败证据；工具链不可用时记录原因并获得用户确认

> 2.5 例外已获用户确认：编写测试后首次运行时本机尚无真实 Python/pytest，命令在测试收集前失败；安装工具链时核心实现已经存在，因此没有补造实施前失败记录。

## 3. 后端核心实现

- [x] 3.1 新增 `app/core/capability.py`，定义稳定的 `Capability` 字符串枚举及中文公共文档字符串
- [x] 3.2 实现固定的配置名称、配置版本、不可变 `LITE_CAPABILITIES` 和派生禁用集合
- [x] 3.3 实现严格的 `is_capability_enabled()`，拒绝未知值和错误参数类型
- [x] 3.4 实现确定性的机器清单生成函数，确保返回快照不泄露或引用内部可变状态
- [x] 3.5 运行能力契约测试并保存全部通过的验证证据

## 4. 边界与组件验证

- [x] 4.1 验证 API、启动生命周期、模块扫描、调度器、命令、工作流和插件加载均未引用新能力模块，确认现有运行行为未改变
- [x] 4.2 验证前端仓库未修改，并记录前端构建消费能力清单属于后续独立变更
- [x] 4.3 验证插件框架和 P115StrmHelper 行为未修改，并记录插件兼容门控属于后续独立变更
- [x] 4.4 验证 requirements、Dockerfile、Compose 和镜像资源未修改，并记录依赖及镜像裁剪属于后续独立变更
- [x] 4.5 确认本变更不需要数据库迁移、`/config` 迁移或真实 115、消息渠道、媒体服务器 Canary 测试

## 5. 质量与文档

- [x] 5.1 对新增测试和能力模块运行项目规定的格式、静态检查及定向 pytest
- [x] 5.2 运行完整 pytest 回归测试；若存在与本变更无关的基线失败，单独记录且不得隐瞒
- [x] 5.3 对照实际公共接口更新 `docs/ARCHITECTURE.md` 中的能力配置落点说明，保持 `PROJECT_BRIEF.md` 和兼容矩阵边界不变
- [x] 5.4 运行 `openspec validate add-lite-capability-profile --type change --strict` 并确认全部工件有效

> 验证记录（Windows、Python 3.12.10）：定向 pytest 为 7 passed；新模块 pylint 为 10.00/10 且零消息；完整 pytest 为 1932 passed、1 skipped、41 failed，失败均位于未修改的 Agent、Docker shell、下载器、消息渠道、本地安装、多语言及媒体刮削测试；全包 pylint 仅在未修改的 `_terminal_session.py` 因 Windows 不提供 `fcntl` 产生 E0401。

## 6. 提交与推送关键节点

- [x] 6.1 汇总代码差异、测试证据和剩余风险，在创建 Git 提交前获得用户批准
- [x] 6.2 仅提交本变更相关文件，并在提交信息中关联 `add-lite-capability-profile`
- [x] 6.3 展示提交内容和本地状态，在向私有 origin 推送前获得用户批准
- [x] 6.4 推送批准的分支并核对远端提交；不创建发布、不构建镜像、不操作 Canary 或生产实例
