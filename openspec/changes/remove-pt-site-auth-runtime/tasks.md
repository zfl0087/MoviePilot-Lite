## 1. 规划与边界

- [x] 1.1 严格验证 `remove-pt-site-auth-runtime` 的 proposal、design、specs 和 tasks 工件
- [x] 1.2 确认范围只包含 PT 站点认证、相关启动资源和三个站点系统任务，不顺带实施模块发现、消息命令、前端或依赖裁剪
- [x] 1.3 在运行代码实施前展示认证等级、配置兼容、插件和 115 隔离方案并取得用户明确批准

## 2. 认证契约测试

- [x] 2.1 新增 `AUTH_SITE` 不再属于有效 Lite 配置且历史输入被忽略的 pytest 用例
- [x] 2.2 新增管理员登录、超级管理员 Token 和资源 Token 固定等级 1 且不读取站点认证的 pytest 用例
- [x] 2.3 扩展错误密码、错误 API Token 和管理员权限拒绝回归，证明移除 PT 认证没有放宽安全边界
- [x] 2.4 新增 115 存储授权路由和 U115 配置不受 PT 认证移除影响的离线契约测试

## 3. 本地认证实现

- [x] 3.1 在安全核心定义共享固定 Lite 认证等级 1，并用定向测试证明结果确定
- [x] 3.2 从 `ConfigModel` 删除 `AUTH_SITE`，验证旧输入被忽略且不修改历史数据库记录
- [x] 3.3 从管理员登录和超级管理员 Token 构造中移除站点等级读取，保持原请求、响应和权限依赖

## 4. 插件认证策略

- [x] 4.1 先增加插件等级 0/1 允许、2/3/99 拒绝的参数化测试
- [x] 4.2 将插件认证判断改为纯本地等级门槛，删除站点在线认证及特殊私钥提升链路
- [x] 4.3 验证等级 1 只代表进入后续兼容检查，不改变插件安装方式或宣称 P115StrmHelper 已自动兼容

## 5. 启动与调度清理

- [x] 5.1 新增模块启动不会调用 `SitesHelper.auth_level`、`check_user()`、认证通知或 `ResourceHelper` 的隔离测试
- [x] 5.2 从模块初始化器移除认证检查、认证通知和 PT 认证/索引资源包启动副作用
- [x] 5.3 新增 Scheduler 服务、Job 和清理计划排除 `cookiecloud`、`user_auth`、`sitedata_refresh`、`siteuserdata` 的测试
- [x] 5.4 从 Scheduler 移除三个站点任务、认证实现和站点历史清理计划，并验证旧运行配置不能恢复
- [x] 5.5 验证保留的事件、消息、插件、元数据、网盘、整理和媒体服务器所有者仍按原顺序启动和关闭

## 6. 文档与范围审查

- [x] 6.1 更新 `PROJECT_BRIEF.md`、`docs/ARCHITECTURE.md` 和 `docs/COMPATIBILITY.md` 的实际实施状态与 `AUTH_SITE` 迁移说明
- [x] 6.2 确认不修改数据库 Schema、前端仓库、依赖文件或 Docker 构建；记录这些边界为后续独立变更
- [x] 6.3 确认源码和日志不包含真实 PT Cookie、115 Token、密码或其他秘密信息

## 7. 验证

- [x] 7.1 运行新增认证、插件、启动、Scheduler、API 授权及 115 隔离定向 pytest
- [x] 7.2 对修改的 Python 模块运行定向 pylint，确认没有新增问题
- [x] 7.3 因修改 `app/core/`、配置和启动流程，运行全量 pytest 并与已知 Windows 基线失败比较
- [x] 7.4 运行 `pylint app/`，区分本变更新问题和已知 Windows `fcntl` 基线问题
- [x] 7.5 严格验证完成后的 OpenSpec；本变更不修改依赖，因此不运行依赖安全扫描
- [x] 7.6 使用完全模拟环境验证旧 `AUTH_SITE` 存在、PT 服务离线时启动和登录正常；真实 115 仅留给候选 Canary 人工验证

## 8. 版本控制检查点

- [x] 8.1 在创建 Git 提交前停止，展示修改、测试结果、已知基线和剩余 PT 引用，取得用户明确批准
- [ ] 8.2 获批后创建单一目的提交，并在推送前再次停止取得明确批准
- [ ] 8.3 获批后仅推送到私有 `origin/lite`；不得推送 `upstream`、发布版本或升级生产实例
