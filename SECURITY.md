# Security Policy

## 支持范围

当前只接收与 `v2.14.5-lite.1-rc.7` 公开预览版及其明确固定源码相关的安全报告。未固定版本、自行修改镜像、第三方插件未验证功能和直接暴露公网造成的问题不在支持承诺内，但仍可提供最小复现信息供评估。

## 报告安全问题

优先使用 GitHub 仓库的私密漏洞报告入口。若入口尚未启用，请创建一个不含漏洞细节和敏感数据的普通 Issue，只说明“需要私密安全报告渠道”，等待维护者提供后续方式。

报告只需包含：

- 受影响的完整 Lite 版本、后端和前端提交。
- 镜像完整标签和摘要。
- 操作系统、CPU 架构和最小必要部署方式。
- 去除敏感值后的最小复现步骤、预期结果和实际结果。
- 已采取的临时缓解措施。

严禁提交或上传：

- 115 Token、Cookie、二维码或分享访问凭据。
- 消息渠道 Token、管理员密码、`API_TOKEN` 或 Docker/GitHub 凭据。
- 数据库、真实 `/config`、完整环境变量、完整日志或未经脱敏的截图。
- 可识别个人媒体、目录结构、用户 ID、IP、公网域名或聊天内容的数据。

公开 Issue 中不要发布可直接利用的攻击步骤。维护者确认修复和披露时间前，不保证预览版本的响应时限。

## 部署边界

- 只使用固定版本和摘要，不使用 `latest`。
- 管理端口不得直接暴露到公网。
- 升级前备份 `/config`，测试和生产不得共用同一可写配置目录。
- 第三方插件需单独评估来源、权限、遥测和网络访问；Lite 核心不替插件提供安全保证。
