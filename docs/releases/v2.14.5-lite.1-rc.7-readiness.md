# MoviePilot Lite v2.14.5-lite.1-rc.7 公开发布就绪审计

> 审计日期：2026-07-22（Asia/Shanghai）
>
> 结论：秘密审计有条件通过；未发现 Lite 自有改动引入的凭据。Docker 构建上下文加固和公开发布准备已经合入 `lite`，前后端 Lite 自有提交范围复扫均为 0 条命中；两个源码仓库已经公开并通过匿名访问验证。

## 1. 审计对象

| 对象 | 固定引用 |
|---|---|
| 后端发布分支 | `origin/lite` / `487b56828f185b8530322aafd0524bd20da42a45` |
| rc.7 后端运行源码 | `429dcd7478cac731e3484c21b1f1fff4412d28c0` |
| 前端发布分支 | `origin/lite` / `4aca2de078cae65b1e83af86a7b4abee04987d03` |
| 官方后端基线 | `8b5524a321c940f337873cefa54ba6a58462f9a4` |
| 官方前端基线 | `435e9ecfdd4febf791fd581b3be9233816cebf7f` |
| 公开镜像 | `docker.io/zfl0087/moviepilot-lite:v2.14.5-lite.1-rc.7` |
| 镜像摘要 | `sha256:f408a816d0a0ec15e175477f9bfc4d80278c3455aa2dbfa06b4fdd83e7a01420` |

## 2. 工具与范围

- Gitleaks `8.30.1`，来自[官方 Release](https://github.com/gitleaks/gitleaks/releases/tag/v8.30.1)。
- Windows x64 压缩包 SHA-256：`d29144deff3a68aa93ced33dddf84b7fdc26070add4aa0f4513094c8332afc4e`，已与官方 `checksums.txt` 匹配。
- 扫描后端和前端当前发布引用、完整祖先历史，以及官方基线到 Lite 发布引用的自有变更范围。
- 只检查命中规则、文件路径、提交和行号；报告和对话不记录疑似秘密值。
- 检查 Git 历史中的数据库、日志、环境文件、私钥、证书、Cookie、Token 和配置路径。
- 检查 GitHub Actions Candidate/Docker Hub Sync 日志、Artifacts、OCI 构建记录、SBOM/Provenance 配置和 Docker 构建上下文。

## 3. 源码与 Git 历史结果

### 3.1 Lite 自有改动

| 范围 | Gitleaks 结果 |
|---|---|
| 后端 `8b5524a...` → `487b5682...` | 0 条 |
| 前端 `435e9ec...` → `4aca2de...` | 0 条 |

未发现 Lite 自有提交引入 Token、Cookie、密码、私钥或真实 `/config` 数据。

### 3.2 完整祖先历史

| 仓库 | 命中 | 规则 | 来源 |
|---|---:|---|---|
| 后端 | 19 | `generic-api-key` | 13 个提交全部包含于 `upstream/v2` |
| 前端 | 9 | `generic-api-key` | 5 个提交全部包含于 `upstream/v2` |

这些命中均继承自已经公开的 MoviePilot 官方上游，不是 Lite 提交产生。分类结果：

- 当前代码中的豆瓣/Trimemedia 常量、Plex 字段名、Ace 语法 Token、公开 VAPID Key 和测试夹具。
- 已删除的上游示例 `config/app.env`、TwoFA 示例数据和旧插件代码。
- 官方初始历史中的 `config/user.db` 只有一个默认用户记录，密码字段是 60 字符哈希；业务表均为空。
- 上游 `P115StrmHelper` 历史数据库的文件和目录表均为空。
- 上游站点资源二进制不含 URL、邮箱、`token`、`cookie`、`secret` 或密码关键字。
- 上游初始日志不含 IP、邮箱、URL 或凭据关键字。

当前发布树不包含数据库、PEM、私钥、P12/PFX、真实 Cookie、真实 `/config` 或个人日志。前端本地忽略的 `dist` 产物不会进入远程仓库；其中的命中只是已跟踪 Ace 配置的构建副本。

## 4. GitHub Actions 与 Artifacts

审计了 Actions 页面现存的全部 Lite 运行：

- Candidate：8 次，运行编号 `#1` 至 `#8`。
- Docker Hub Sync：7 次，运行编号 `#1` 至 `#6`、`#9`。
- 15 个 Job 均为 `success`。

对所有 15 份完整 Job 日志执行脱敏模式检测：

- GitHub/Docker Token：0。
- PEM 私钥：0。
- AWS Access Key：0。
- 115 `UID`/`CID`/`SEID` Cookie：0。
- 长 Token、密码、Cookie、Secret 或 API Key 赋值：0。
- 日志中的 `***` 是 GitHub Secrets 正常掩码。

所有 Artifacts 的名称和大小均符合工作流合同：Candidate 只包含 Docker Build Record、前端哈希清单和候选报告；Sync 只包含同步报告。最早和最新 Artifact 均已按 GitHub 返回的 SHA-256 校验并抽样解包扫描：

- Candidate evidence：0 条命中。
- Docker Hub sync evidence：0 条命中。
- Docker Build Record：每份 10 条 `GPG_KEY` 命中，均为 Python 基础镜像公开的 GPG 公钥标识，不是私钥或部署凭据。

rc.7 Candidate 报告固定记录了正确的前后端提交、双架构和镜像摘要。rc.7 Sync 报告的源/目标摘要一致。该次 Sync 发生在同步工作流增加前后端 SHA 字段之前，因此旧 Artifact 没有这两个字段；Candidate 报告和仓库发布记录已经提供完整追溯，后续 Sync 将使用新字段。

## 5. OCI、SBOM 与构建上下文

- rc.7 OCI 索引同时包含 `linux/amd64` 和 `linux/arm64`。
- 两个平台均存在独立 attestation manifest。
- Candidate 工作流启用 `provenance: mode=max` 和 `sbom: true`。
- Docker Build Record 摘要与 GitHub Artifact 元数据一致。
- Candidate 通过 GitHub Actions 的干净、固定 SHA 检出构建；前端只把固定提交生成的 `dist` 复制为候选输入。
- 构建没有使用 Docker `secrets` 或 `secret-files`，也没有把 GitHub App 私钥作为 build arg 传入。

### 5.1 已修复门禁

`.dockerignore` 已在原有数据库、日志、测试、Git 元数据和虚拟环境规则之外，显式排除以下本地敏感文件：

- `.env`、`.env.*`。
- `*.pem`、`*.key`、`*.p12`、`*.pfx`、`*.jks`、`*.keystore`。
- `*.credentials`、`credentials.*`、`secrets.*`、`*.bak`、`*.backup`。

`tests/test_lite_docker_image_contract.py` 增加了精确集合合同测试，防止这些规则在后续同步中被意外删除。现有 rc.7 Actions 构建使用干净检出，本来未受该缺口影响；该加固进一步保护公开后用户或维护者执行本地 `docker build` 的场景。

## 6. 公开访问验证与后续事项

匿名验证未携带 GitHub 或 Docker Hub 账户凭据，结果如下：

- 前后端仓库主页和固定提交页面均返回 HTTP 200，且没有跳转至登录页。
- 后端 LICENSE、README、公开发布指南、发布记录和 Candidate 工作流均可通过固定提交 Raw URL 读取。
- 前端 LICENSE 和 `package.json` 均可通过固定提交 Raw URL 读取。
- Docker Registry 匿名拉取令牌和 rc.7 OCI 索引均返回 HTTP 200；摘要匹配 `sha256:f408a816d0a0ec15e175477f9bfc4d80278c3455aa2dbfa06b4fdd83e7a01420`，包含 `amd64`、`arm64` 及 attestation 使用的 `unknown` 平台清单。
- GitHub 匿名 REST API 因共享出口公共限流未作为验证依据；普通公开页面和 Raw 文件验证不受影响。
- Docker Hub 简短描述和完整 Overview 已更新，并在公开页面确认后端源码、前端源码、安装文档和兼容性文档链接可见。

后续仍需完成：

1. 创建 GitHub Pre-release 时继续保留 rc.7 的四项未验证声明：真实 115 分享转存、完整 STRM、消息渠道回传、媒体服务器刷新。

未经单独批准，不创建公开标签或 Release。
